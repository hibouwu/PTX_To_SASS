# tcgen05.mma 最终报告

本目录只保存适合直接阅读和纳入版本控制的最终报告。

运行 `../check_all.sh` 后，完整 cubin、SASS、逐配对 JSONL 和中间日志写入
`../results/`；最终中文上下文差分报告会同步生成到
`tcgen05_mma_上下文差分报告.md`。

`results/` 中的 `.cubin` 和 `.sass` 文件会被 Git 忽略；其余报告和元数据
仍可按需纳入版本控制。
