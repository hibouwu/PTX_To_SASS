.version 9.0
.target sm_110a
.address_size 64
.file 1 "thor_sreg_threadindex_0002_caa392fc.ptx"


// OPCODE mov
// COORDINATES {"consumer":"address","context":"baseline","dim":"x","kind":"tid"}
.visible .entry thor_sreg_threadindex_0002_caa392fc(
    .param .u64 p_base,
    .param .u64 p_out
)
{
    .reg .b32 %r0, %r1, %r2;
    .reg .b64 %basep, %outp, %rd0, %addr;
    .reg .u32 %v;
    .reg .pred %p;
    // PREPARATION_BEGIN
    ld.param.u64 %basep, [p_base];
    ld.param.u64 %outp, [p_out];
    // PREPARATION_END
    // TARGET_PATTERN_BEGIN
    mov.u32 %r0, %tid.x;
    // TARGET_PATTERN_END
    // OBSERVATION_BEGIN
    mul.wide.u32 %rd0, %r0, 4;
    add.u64 %addr, %basep, %rd0;
    ld.global.u32 %v, [%addr];
    st.global.u32 [%outp], %v;
    // OBSERVATION_END
    ret;
}
