# 指令族实验模板

## 元数据

```yaml
family_id:
family_name:
owner:
status: NOT_STARTED
environment_id:
scope_version:
```

## 研究边界

- 目标 PTX 指令形态：
- 明确排除：
- 最大局部数据流节点数：
- 最大基本块数：
- 最大 live value 数：
- 是否覆盖函数、链接或 LTO：

## 可操纵因素

列出 PTX 生成器能够直接控制的因素、精确水平和合法性约束。

## 派生量与 manipulation check

列出需要从编译结果验证的量，例如实际 GPR/UR/UP、live range、spill、最终 consumer
距离和目标是否被消除。这些量不能直接当作已实现的输入因素。

## 高风险交互

预注册该族必须覆盖的二阶、三阶或更高阶组合，不以单因素是否显效为前提。

## 观测与归属

- 目标 effect slice：
- exact fingerprint：
- allocation fingerprint：
- semantic fingerprint：
- treatment/control：
- 观察 sink 及其稳健性检查：

## 语义验证

- 定义域与未定义行为排除：
- host 或 metamorphic oracle：
- 特殊生命周期或参与条件：

## 停止条件

- 有限输入空间或组合覆盖目标：
- 允许的失败状态：
- 阻止 `VALIDATED` 的条件：

## 结果

- 已观察候选：
- 最小 witness：
- 未覆盖空间：
- 无法归属：
- 可被新 testcase 证伪的结论：

