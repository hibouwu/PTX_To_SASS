.version 9.0
.target sm_110a
.address_size 64
.file 1 "thor_act_tanh_0005_c1fe911f.ptx"


// OPCODE tanh.approx
// COORDINATES {"consumer":"mul","context":"baseline","dtype":"f16"}
.visible .entry thor_act_tanh_0005_c1fe911f(
    .param .u64 p_in,
    .param .u64 p_in2,
    .param .u64 p_out
)
{
    .reg .b64 %rin, %rin2, %rout;
    .reg .b16 %a, %b, %d, %e;
    // PREPARATION_BEGIN
    ld.param.b64 %rin, [p_in];
    ld.param.b64 %rin2, [p_in2];
    ld.param.b64 %rout, [p_out];
    ld.global.b16 %a, [%rin];
    ld.global.b16 %b, [%rin2];
    // PREPARATION_END
    // TARGET_PATTERN_BEGIN
    tanh.approx.f16 %d, %a;
    // TARGET_PATTERN_END
    // OBSERVATION_BEGIN
    mul.f16 %e, %d, %b;
    st.global.b16 [%rout], %e;
    // OBSERVATION_END
    ret;
}
