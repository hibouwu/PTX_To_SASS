.version 9.0
.target sm_110a
.address_size 64
.file 1 "thor_act_tanh_0012_bf447e3f.ptx"


// OPCODE tanh.approx
// COORDINATES {"consumer":"cvt","context":"baseline","dtype":"f16x2"}
.visible .entry thor_act_tanh_0012_bf447e3f(
    .param .u64 p_in,
    .param .u64 p_out
)
{
    .reg .b64 %rin, %rout;
    .reg .b32 %a, %d;
    .reg .b16 %dlo, %dhi;
    .reg .f32 %flo, %fhi, %fsum;
    // PREPARATION_BEGIN
    ld.param.b64 %rin, [p_in];
    ld.param.b64 %rout, [p_out];
    ld.global.b32 %a, [%rin];
    // PREPARATION_END
    // TARGET_PATTERN_BEGIN
    tanh.approx.f16x2 %d, %a;
    // TARGET_PATTERN_END
    // OBSERVATION_BEGIN
    mov.b32 {%dlo, %dhi}, %d;
    cvt.f32.f16 %flo, %dlo;
    cvt.f32.f16 %fhi, %dhi;
    add.f32 %fsum, %flo, %fhi;
    st.global.f32 [%rout], %fsum;
    // OBSERVATION_END
    ret;
}
