.version 9.0
.target sm_110a
.address_size 64
.file 1 "thor_act_tanh_0010_a0c83cfe.ptx"


// OPCODE tanh.approx
// COORDINATES {"consumer":"direct","context":"baseline","dtype":"f16x2"}
.visible .entry thor_act_tanh_0010_a0c83cfe(
    .param .u64 p_in,
    .param .u64 p_out
)
{
    .reg .b64 %rin, %rout;
    .reg .b32 %a, %d;
    // PREPARATION_BEGIN
    ld.param.b64 %rin, [p_in];
    ld.param.b64 %rout, [p_out];
    ld.global.b32 %a, [%rin];
    // PREPARATION_END
    // TARGET_PATTERN_BEGIN
    tanh.approx.f16x2 %d, %a;
    // TARGET_PATTERN_END
    // OBSERVATION_BEGIN
    st.global.b32 [%rout], %d;
    // OBSERVATION_END
    ret;
}
