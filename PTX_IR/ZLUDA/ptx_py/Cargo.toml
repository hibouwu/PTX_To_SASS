[package]
name = "ptx_py"
version = "0.1.0"
edition = "2021"

[lib]
name = "ptx_py"
crate-type = ["cdylib"]  # .so for Python import

[dependencies]
ptx = { path = "../ptx" }
ptx_parser = { path = "../ptx_parser" }
pyo3 = { version = "0.22", features = ["extension-module"] }
