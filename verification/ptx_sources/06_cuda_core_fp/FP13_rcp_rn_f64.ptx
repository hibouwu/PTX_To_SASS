.version 9.3
.target sm_100a
.address_size 64

// FP13: rcp.rn.f64 (HIGH RISK: Newton-Raphson?)
.visible .entry test_rcp_rn_f64() {
    .reg .f64 %fd0, %fd1;

    mov.f64 %fd0, 0d4000000000000000;

    // === 待测指令 ===
    rcp.rn.f64 %fd1, %fd0;

    ret;
}
