.version 9.3
.target sm_100a
.address_size 64

// FP11: rcp.approx.f32 (SFU)
.visible .entry test_rcp_approx_f32() {
    .reg .f32 %f0, %f1;

    mov.f32 %f0, 0f40000000;

    // === 待测指令 ===
    rcp.approx.f32 %f1, %f0;

    ret;
}
