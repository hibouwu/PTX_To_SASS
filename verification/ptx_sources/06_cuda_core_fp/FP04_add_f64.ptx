.version 9.3
.target sm_100a
.address_size 64

// FP04: add.f64
.visible .entry test_add_f64() {
    .reg .f64 %fd0, %fd1, %fd2;

    mov.f64 %fd0, 0d3FF0000000000000;
mov.f64 %fd1, 0d4000000000000000;

    // === 待测指令 ===
    add.f64 %fd2, %fd0, %fd1;

    ret;
}
