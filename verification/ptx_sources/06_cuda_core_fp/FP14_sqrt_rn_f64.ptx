.version 9.3
.target sm_100a
.address_size 64

// FP14: sqrt.rn.f64 (HIGH RISK: multi-step?)
.visible .entry test_sqrt_rn_f64() {
    .reg .f64 %fd0, %fd1;

    mov.f64 %fd0, 0d4010000000000000;

    // === 待测指令 ===
    sqrt.rn.f64 %fd1, %fd0;

    ret;
}
