.version 9.3
.target sm_100a
.address_size 64

// C06: cvt.u32.u64 (truncate)
.visible .entry test_cvt_u32_u64() {
    .reg .u64 %rd0;
    .reg .u32 %r0;

    mov.u64 %rd0, 0x00000000DEADBEEF;

    // === 待测指令 ===
    cvt.u32.u64 %r0, %rd0;

    ret;
}
