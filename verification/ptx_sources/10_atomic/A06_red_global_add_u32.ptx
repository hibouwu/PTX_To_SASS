.version 9.3
.target sm_100a
.address_size 64

.visible .entry test_red_global_add_u32(
    .param .u64 p_addr
) {
    .reg .u64 %addr;
    .reg .u32 %r0;

    ld.param.u64 %addr, [p_addr];
    mov.u32 %r0, 1;

    // === 待测指令 ===
    red.global.add.u32 [%addr], %r0;

    ret;
}
