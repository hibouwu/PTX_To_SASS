.version 9.3
.target sm_100a
.address_size 64

.shared .align 4 .u32 smem_data[256];

.visible .entry test_st_shared_b32() {
    .reg .u32 %r0;
    mov.u32 %r0, 42;

    // === 待测指令 ===
    st.shared.b32 [smem_data], %r0;

    ret;
}
