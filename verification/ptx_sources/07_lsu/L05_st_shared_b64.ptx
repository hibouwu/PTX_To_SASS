.version 9.3
.target sm_100a
.address_size 64

.shared .align 8 .u64 smem_data64[128];

.visible .entry test_st_shared_b64() {
    .reg .b64 %rd0;
    mov.b64 %rd0, 42;

    // === 待测指令 ===
    st.shared.b64 [smem_data64], %rd0;

    ret;
}
