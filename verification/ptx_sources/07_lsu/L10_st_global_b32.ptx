.version 9.3
.target sm_100a
.address_size 64

.visible .entry test_st_global_b32(
    .param .u64 p_addr
) {
    .reg .u64 %addr;
    .reg .u32 %r0;

    ld.param.u64 %addr, [p_addr];
    mov.u32 %r0, 42;

    // === 待测指令 ===
    st.global.b32 [%addr], %r0;

    ret;
}
