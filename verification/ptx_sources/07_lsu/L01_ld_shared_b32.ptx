.version 9.3
.target sm_100a
.address_size 64

.shared .align 4 .u32 smem_data[256];

.visible .entry test_ld_shared_b32() {
    .reg .u32 %r0;

    // === 待测指令 ===
    ld.shared.b32 %r0, [smem_data];

    ret;
}
