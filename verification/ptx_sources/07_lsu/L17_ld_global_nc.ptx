.version 9.3
.target sm_100a
.address_size 64

.visible .entry test_ld_global_nc(
    .param .u64 p_addr
) {
    .reg .u64 %addr;
    .reg .u32 %r0;

    ld.param.u64 %addr, [p_addr];

    // === 待测指令 ===
    ld.global.nc.b32 %r0, [%addr];

    ret;
}
