.version 9.3
.target sm_100a
.address_size 64

.shared .align 8 .u32 smem_data[256];

.visible .entry test_ld_shared_v2b32() {
    .reg .b32 %r0, %r1;

    // === 待测指令 ===
    ld.shared.v2.b32 {%r0, %r1}, [smem_data];

    ret;
}
