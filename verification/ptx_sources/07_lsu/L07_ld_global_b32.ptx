.version 9.3
.target sm_100a
.address_size 64

.visible .entry test_ld_global_b32(
    .param .u64 p_addr
) {
    .reg .u64 %addr;
    .reg .u32 %r0;

    ld.param.u64 %addr, [p_addr];

    // === 待测指令 ===
    ld.global.b32 %r0, [%addr];

    ret;
}
