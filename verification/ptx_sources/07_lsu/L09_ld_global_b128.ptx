.version 9.3
.target sm_100a
.address_size 64

.visible .entry test_ld_global_b128(
    .param .u64 p_addr
) {
    .reg .u64 %addr;
    .reg .b32 %r0, %r1, %r2, %r3;

    ld.param.u64 %addr, [p_addr];

    // === 待测指令 ===
    ld.global.v4.b32 {%r0, %r1, %r2, %r3}, [%addr];

    ret;
}
