.version 9.3
.target sm_100a
.address_size 64

.shared .align 8 .u64 smem_data64[128];

.visible .entry test_ld_shared_b64() {
    .reg .b64 %rd0;

    // === 待测指令 ===
    ld.shared.b64 %rd0, [smem_data64];

    ret;
}
