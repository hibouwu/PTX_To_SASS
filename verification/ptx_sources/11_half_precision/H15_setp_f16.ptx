.version 9.3
.target sm_100a
.address_size 64

// H15: setp.gt.f16
.visible .entry test_setp_f16() {
    .reg .f16 %h0, %h1;
    .reg .pred %p0;

    mov.b16 %h0, 0x4000;
mov.b16 %h1, 0x3C00;

    // === 待测指令 ===
    setp.gt.f16 %p0, %h0, %h1;

    ret;
}
