.version 9.3
.target sm_100a
.address_size 64

// BT10: fns.b32 (find n-th set bit)
.visible .entry test_fns_b32() {
    .reg .b32 %r0, %r1;
    .reg .u32 %r2;

    mov.b32 %r0, 0xFF00FF00;
mov.b32 %r1, 3;

    // === 待测指令 ===
    fns.b32 %r2, %r0, 0, %r1;

    ret;
}
