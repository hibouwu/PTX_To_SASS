.version 9.3
.target sm_100a
.address_size 64

// BT07: popc.b32 (population count)
.visible .entry test_popc_b32() {
    .reg .b32 %r0;
    .reg .u32 %r1;

    mov.b32 %r0, 0xFF00FF00;

    // === 待测指令 ===
    popc.b32 %r1, %r0;

    ret;
}
