.version 9.3
.target sm_100a
.address_size 64

// ACT08: tanh.approx.f32 (sm_100 native)
.visible .entry test_tanh_f32() {
    .reg .f32 %f0, %f1;

    mov.f32 %f0, 0f3F800000;

    // === 待测指令 ===
    tanh.approx.f32 %f1, %f0;

    ret;
}
