.version 9.3
.target sm_100a
.address_size 64

// ACT05: ex2.approx.f16 (softmax exp)
.visible .entry test_ex2_f16() {
    .reg .f16 %h0, %h1;

    mov.b16 %h0, 0x3C00;

    // === 待测指令 ===
    ex2.approx.f16 %h1, %h0;

    ret;
}
