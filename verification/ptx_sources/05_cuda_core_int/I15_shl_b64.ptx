.version 9.3
.target sm_100a
.address_size 64

// I15: shl.b64
.visible .entry test_shl_b64() {
    .reg .b64 %rd0, %rd1;

    mov.b64 %rd0, 42;

    // === 待测指令 ===
    shl.b64 %rd1, %rd0, 4;

    ret;
}
