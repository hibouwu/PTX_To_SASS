.version 9.3
.target sm_100a
.address_size 64

// I09: mad.wide.u32 (32x32+64->64)
.visible .entry test_mad_wide_u32() {
    .reg .u32 %r0, %r1;
    .reg .u64 %rd0;

    mov.u32 %r0, 42;
mov.u32 %r1, 7;
mov.u64 %rd0, 100;

    // === 待测指令 ===
    mad.wide.u32 %rd0, %r0, %r1, %rd0;

    ret;
}
