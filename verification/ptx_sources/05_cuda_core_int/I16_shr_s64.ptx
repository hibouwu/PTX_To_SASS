.version 9.3
.target sm_100a
.address_size 64

// I16: shr.s64
.visible .entry test_shr_s64() {
    .reg .s64 %rd0, %rd1;

    mov.s64 %rd0, 42;

    // === 待测指令 ===
    shr.s64 %rd1, %rd0, 4;

    ret;
}
