.version 9.3
.target sm_100a
.address_size 64

// I07: mul.lo.s64
.visible .entry test_mul_lo_s64() {
    .reg .s64 %rd0, %rd1, %rd2;

    mov.s64 %rd0, 42;
mov.s64 %rd1, 7;

    // === 待测指令 ===
    mul.lo.s64 %rd2, %rd0, %rd1;

    ret;
}
