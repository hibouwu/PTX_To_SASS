.version 9.3
.target sm_100a
.address_size 64

// W02: shfl.sync.up.b32
.visible .entry test_shfl_up() {
    .reg .b32 %r0, %r1;

    mov.b32 %r0, %tid.x;

    // === 待测指令 ===
    shfl.sync.up.b32 %r1, %r0, 1, 0, 0xFFFFFFFF;

    ret;
}
