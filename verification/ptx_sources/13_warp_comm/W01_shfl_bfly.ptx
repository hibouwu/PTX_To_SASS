.version 9.3
.target sm_100a
.address_size 64

// W01: shfl.sync.bfly.b32 (butterfly)
.visible .entry test_shfl_bfly() {
    .reg .b32 %r0, %r1;

    mov.b32 %r0, %tid.x;

    // === 待测指令 ===
    shfl.sync.bfly.b32 %r1, %r0, 1, 0x1f, 0xFFFFFFFF;

    ret;
}
