.version 9.3
.target sm_100a
.address_size 64

// W11: vote.sync.any.pred
.visible .entry test_vote_any() {
    .reg .pred %p0, %p1;

    setp.eq.u32 %p0, %tid.x, 0;

    // === 待测指令 ===
    vote.sync.any.pred %p1, %p0, 0xFFFFFFFF;

    ret;
}
