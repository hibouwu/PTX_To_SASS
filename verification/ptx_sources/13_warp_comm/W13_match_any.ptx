.version 9.3
.target sm_100a
.address_size 64

// W13: match.sync.any.b32
.visible .entry test_match_any() {
    .reg .b32 %r0, %r1;

    mov.b32 %r0, %tid.x;

    // === 待测指令 ===
    match.sync.any.b32 %r1, %r0, 0xFFFFFFFF;

    ret;
}
