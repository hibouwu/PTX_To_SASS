.version 9.3
.target sm_100a
.address_size 64

// W04: shfl.sync.idx.b32
.visible .entry test_shfl_idx() {
    .reg .b32 %r0, %r1;

    mov.b32 %r0, %tid.x;

    // === 待测指令 ===
    shfl.sync.idx.b32 %r1, %r0, 0, 0x1f, 0xFFFFFFFF;

    ret;
}
