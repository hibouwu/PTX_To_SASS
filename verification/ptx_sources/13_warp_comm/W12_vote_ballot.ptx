.version 9.3
.target sm_100a
.address_size 64

// W12: vote.sync.ballot.b32
.visible .entry test_vote_ballot() {
    .reg .pred %p0;
    .reg .b32 %r0;

    setp.gt.u32 %p0, %tid.x, 15;

    // === 待测指令 ===
    vote.sync.ballot.b32 %r0, %p0, 0xFFFFFFFF;

    ret;
}
