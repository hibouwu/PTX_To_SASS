.version 9.3
.target sm_100a
.address_size 64

// W14: elect.sync (leader election)
.visible .entry test_elect_sync() {
    .reg .b32 %r0;
    .reg .pred %p0;



    // === 待测指令 ===
    elect.sync %r0|%p0, 0xFFFFFFFF;

    ret;
}
