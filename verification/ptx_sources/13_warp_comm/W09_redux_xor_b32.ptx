.version 9.3
.target sm_100a
.address_size 64

// W09: redux.sync.xor.b32
.visible .entry test_redux_xor_b32() {
    .reg .b32 %r0, %r1;

    mov.b32 %r0, %tid.x;

    // === 待测指令 ===
    redux.sync.xor.b32 %r1, %r0, 0xFFFFFFFF;

    ret;
}
